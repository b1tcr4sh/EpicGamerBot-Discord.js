var vox = {
    brainCellCount = 2,
    fellonyCount = 10000
};



if (vox.brainCellCount <= 10) {
    if (10 === 10) {
        if (true) {
            ReplyToGithubRepoIssue();
        }
    }
    if (10 != 10) {
        // This is mathematically impossible
    }
    if (vox.fellonyCount >= 1) {
        SendFBIToVoxHouse();
    }
}




function ReplyToGithubRepoIssue() {
    // 🖕
}

function SendFBIToVoxHouse() {
    // I don't know what to put here
}